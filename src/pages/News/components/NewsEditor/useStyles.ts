import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  saveButton: {
    marginLeft: '15px',
  },
  deleteButton: {
    borderColor: '#F66363',
    color: '#F66363',
  },
})

export default useStyles
